import React from "react";
import { Text, Button, View, StyleSheet } from "react-native";

const LiveCallButtons = ({
  setMute,
  setEndCall,
  setAlert,
  showScreenShare,
}) => {
  //Buttons
  <View>
    <Button>End Call</Button>
    <Button>Mute</Button>
    <Button>Alert</Button>
    <Button>ShowHide</Button>
  </View>;
};
